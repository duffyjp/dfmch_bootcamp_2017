<%= form_with(model: loan, local: true) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :person_id %>
    <%= form.select :person_id, options_from_collection_for_select(Person.all, :id, :to_s) %>

  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.select :book_id, options_from_collection_for_select(Book.order(:title), :id, :to_s) %>
  </div>

  <div class="field">
    <%= form.label :due_date %>
    <%= form.date_select :due_date, id: :loan_due_date %>
  </div>

  <div class="field">
    <%= form.label :return_date %>
    <%= form.date_select :return_date, id: :loan_return_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
